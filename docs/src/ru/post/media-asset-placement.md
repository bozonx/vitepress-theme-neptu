---
title: Где хранить изображения и медиа
description: >
  Три поддерживаемых способа размещения медиафайлов — общая папка public,
  рядом с markdown-файлом или папка на статью с подпапкой media — и какой
  выбрать.
date: 2025-07-30T19:00:00Z
authorId: ivan-k
tags:
  - media
  - guide
descrAsPreview: true
translations:
  en: /en/post/media-asset-placement
---

Тема поддерживает три способа организации изображений и других медиафайлов.
Все три работают и для обложек (frontmatter `cover`), и для изображений в
тексте (markdown `![alt](src)`). Выберите тот, который подходит вашему
рабочему процессу.

## 1. Общая папка public

Классический подход VitePress. Все медиа лежат в `src/public/` и
указываются абсолютным путём от корня public.

```text
src/
├─ public/
│  └─ img/
│     ├─ cover.jpg
│     └─ screenshot.png
└─ ru/
   └─ post/
      └─ my-article.md
```

```yaml
# фронтматер
cover: /img/cover.jpg
```

```md
<!-- тело статьи -->
![Скриншот](/img/screenshot.png)
```

**Когда использовать**: небольшие блоги с малым количеством изображений,
общие ассеты для нескольких постов (логотипы, иконки) или когда удобнее
держать всю медиа в одной папке.

## 2. Рядом с markdown-файлом

Изображения лежат рядом с `.md`-файлом и указываются относительным путём.
Тема автоматически определяет размеры при сборке.

```text
src/
└─ ru/
   └─ post/
      ├─ my-article.md
      ├─ cover.jpg
      └─ screenshot.png
```

```yaml
# фронтматер — относительный путь
cover: ./cover.jpg
```

```md
<!-- тело статьи — относительный путь -->
![Скриншот](./screenshot.png)
```

**Когда использовать**: туториалы со скриншотами, посты, где хочется держать
всё в одном месте. Удаление `.md`-файла удаляет и его изображения.

## 3. Папка на статью с подпапкой media

Каждая статья получает свою директорию. Markdown-файл называется `index.md`,
а медиа лежат в подпапке (обычно `media`). С включённым `cleanUrls: true`
(по умолчанию) URL остаётся чистым: `/ru/post/my-article/`.

```text
src/
└─ ru/
   └─ post/
      └─ my-article/
         ├─ index.md
         └─ media/
            ├─ cover.jpg
            ├─ diagram.svg
            └─ photo.png
```

```yaml
# фронтматер — относительно index.md
cover: ./media/cover.jpg
```

```md
<!-- тело статьи — относительно index.md -->
![Диаграмма](./media/diagram.svg)
![Фото](./media/photo.png)
```

**Когда использовать**: статьи с большим количеством изображений, лонгриды
со множеством скриншотов, когда хочется переносить статью (текст + медиа)
одной папкой.

## Сравнение

| Подход | Формат пути | Портативность | Авто-размеры | Подходит для |
|---|---|---|---|---|
| Общая папка public | `/img/foo.png` | Нет | Да | Небольшие блоги, общие ассеты |
| Рядом с .md | `./foo.png` | Да | Да | Туториалы, однофайловые посты |
| Папка на статью | `./media/foo.png` | Да | Да | Медиа-тяжёлые, лонгриды |

Все три подхода получают автоматические `width`/`height` и для обложек, и
для отдельных изображений в тексте (тех, что оборачиваются в `<figure>`).

## Можно совмещать

Вы не привязаны к одному подходу. Распространённый паттерн:

- **Общие ассеты** (логотип, favicon, OG-изображение по умолчанию) в `src/public/`
- **Медиа статей** — рядом с файлом или в папке на статью

```md
<!-- общий логотип из public -->
![Логотип сайта](/img/logo.svg)

<!-- локальный скриншот рядом со статьёй -->
![Шаг 1](./screenshot-1.png)
```

## Медиа-компоненты

Встроенные компоненты темы (`YoutubeVideo`, `VideoFile`, `AudioFile`,
`FileDownload`) тоже работают со всеми тремя подходами. Для локальных
файлов используйте абсолютные пути из public:

```md
<VideoFile url="/media/demo.mp4" filename="Демо-видео" />
```

Для co-located видео/аудио укажите относительный путь так же, как для
изображений — VitePress резолвит его при сборке.

## Рекомендация

- **Начало работы**: используйте `src/public/` — проще всего, без сюрпризов.
- **Растущий блог с множеством изображений**: переходите на co-located или
  папку на статью для новых постов. Существующие посты в `public/`
  продолжат работать.
- **Командная / CMS-работа**: папка на статью упрощает ревью и импорт —
  каждая статья самодостаточна.
