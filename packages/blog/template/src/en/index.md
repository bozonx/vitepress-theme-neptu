---
layout: home
---

Welcome! This text sits between the hero and the home sections — replace it or
delete it. The hero copy lives in `src/en/_site.yaml`, and the sections below
are configured in `src/site.yaml` under `themeConfig.home.sections`.
