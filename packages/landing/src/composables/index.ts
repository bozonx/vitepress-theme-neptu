export {
  useLandingStyle,
  STYLE_PRESETS,
  STYLE_STORAGE_KEY,
  DEFAULT_STYLE,
  type StylePresetOption,
} from './useLandingStyle.ts'

/** Color axis of the theme — shared with the blog theme. */
export {
  useColorTheme,
  COLOR_THEMES,
  type ColorThemeOption,
} from 'vitepress-theme-neptu-blog/composables'
